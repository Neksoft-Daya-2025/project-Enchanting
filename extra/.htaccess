RewriteEngine On
RewriteBase /

# Serve React Router SPA: if request isn't a real file/dir, route to index.html
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.html [L]

